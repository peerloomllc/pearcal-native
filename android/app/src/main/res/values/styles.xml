<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">true</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">#ffffff</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@color/iconBackground</item>
    <item name="android:statusBarColor">@color/iconBackground</item>
    <item name="android:navigationBarColor">@color/iconBackground</item>
    <!-- Android 12+ system splash: without this the launcher icon renders on a
         white background. Match the adaptive icon background so the icon sits
         on near-black with no white border. -->
    <item name="android:windowSplashScreenBackground" tools:targetApi="31">@color/iconBackground</item>
  </style>
</resources>